# Examples

The examples show the shape of a deployment without containing a real
inventory, user identity, private service URL, certificate, or credential.
Copy them into a separate inventory repository and review every variable
before applying the role.
