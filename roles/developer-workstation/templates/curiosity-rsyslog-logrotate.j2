# Managed by Ansible — do not edit manually.
# Replaces the package-owned /etc/logrotate.d/rsyslog (diverted to .dpkg-dist).
# Adds maxsize + daily cadence to prevent unbounded syslog growth between rotations.
/var/log/syslog
/var/log/auth.log
/var/log/kern.log
/var/log/mail.log
/var/log/ufw.log
/var/log/cron.log
/var/log/user.log
{
    su root syslog
    daily
    rotate {{ logrotate_rotate_count }}
    maxsize {{ logrotate_maxsize }}
    missingok
    notifempty
    compress
    delaycompress
    sharedscripts
    postrotate
        /usr/lib/rsyslog/rsyslog-rotate
    endscript
}
