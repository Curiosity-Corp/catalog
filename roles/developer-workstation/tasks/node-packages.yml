---
# =============================================================================
# Node.js Global Packages
# =============================================================================
# Installed via npm global. Node.js itself is installed via APT (packages.yml).
# =============================================================================

- name: Install npm global packages
  community.general.npm:
    name: "{{ item }}"
    global: true
    state: present
  loop:
    - pnpm
    - yarn
    - typescript
    - ts-node
    - tsx
    - turbo
    - eslint
    - prettier
    - "@mermaid-js/mermaid-cli"
  register: _npm_result
  failed_when:
    - _npm_result is failed
    - "'EACCES' not in (_npm_result.msg | default(''))"
