---
# =============================================================================
# Security Tools
# =============================================================================
# Container security, secrets management, and certificate tools
# =============================================================================

- name: Include internal CA trust
  ansible.builtin.include_tasks: internal-cas.yml

# --- Trivy ---
- name: Download latest Trivy
  ansible.builtin.get_url:
    url: >-
      {{
        (latest_github_releases.trivy.assets
        | selectattr('name', 'match', '^trivy_.*_Linux-64bit[.]tar[.]gz$')
        | first).browser_download_url
      }}
    dest: "{{ workstation_update_artifact_cache_dir }}/trivy-{{ (latest_github_releases.trivy.assets | selectattr('name', 'match', '^trivy_.*_Linux-64bit[.]tar[.]gz$') | first).digest | regex_replace('^sha256:', '') }}.archive"
    checksum: >-
      {{ (latest_github_releases.trivy.assets
          | selectattr('name', 'match', '^trivy_.*_Linux-64bit[.]tar[.]gz$')
          | first).digest }}
    mode: "0644"
    force: false

- name: Install latest Trivy
  ansible.builtin.unarchive:
    src: "{{ workstation_update_artifact_cache_dir }}/trivy-{{ (latest_github_releases.trivy.assets | selectattr('name', 'match', '^trivy_.*_Linux-64bit[.]tar[.]gz$') | first).digest | regex_replace('^sha256:', '') }}.archive"
    dest: /usr/local/bin
    remote_src: true
    include: [trivy]

# --- kubeseal (Sealed Secrets) ---
- name: Download latest kubeseal
  ansible.builtin.get_url:
    url: >-
      {{
        (latest_github_releases.kubeseal.assets
        | selectattr('name', 'match', '^kubeseal-.*-linux-amd64[.]tar[.]gz$')
        | first).browser_download_url
      }}
    dest: "{{ workstation_update_artifact_cache_dir }}/kubeseal-{{ (latest_github_releases.kubeseal.assets | selectattr('name', 'match', '^kubeseal-.*-linux-amd64[.]tar[.]gz$') | first).digest | regex_replace('^sha256:', '') }}.archive"
    checksum: >-
      {{ (latest_github_releases.kubeseal.assets
          | selectattr('name', 'match', '^kubeseal-.*-linux-amd64[.]tar[.]gz$')
          | first).digest }}
    mode: "0644"
    force: false

- name: Install latest kubeseal
  ansible.builtin.unarchive:
    src: "{{ workstation_update_artifact_cache_dir }}/kubeseal-{{ (latest_github_releases.kubeseal.assets | selectattr('name', 'match', '^kubeseal-.*-linux-amd64[.]tar[.]gz$') | first).digest | regex_replace('^sha256:', '') }}.archive"
    dest: /usr/local/bin
    remote_src: true
    include: [kubeseal]
    extra_opts: []

# =============================================================================
# Post-Quantum age + SOPS
# =============================================================================
# These tools follow the current upstream releases. The latest age release is
# used for PQ hybrid key support; there is no checked-in compatibility release
# that can cause a workstation to be rolled back.
# =============================================================================

- name: Resolve latest security tool release assets
  ansible.builtin.set_fact:
    _latest_age_asset: >-
      {{
        latest_github_releases.age.assets
        | selectattr('name', 'match', '^age-v.*-linux-amd64\.tar\.gz$')
        | first
      }}
    _latest_sops_asset: >-
      {{
        latest_github_releases.sops.assets
        | selectattr('name', 'match', '^sops-v.*\.linux\.amd64$')
        | first
      }}
    _latest_cosign_asset: >-
      {{ latest_github_releases.cosign.assets
         | selectattr('name', 'equalto', 'cosign-linux-amd64') | first }}
    _latest_step_asset: >-
      {{ latest_github_releases.step.assets
         | selectattr('name', 'equalto', 'step_linux_amd64.tar.gz') | first }}

- name: Require latest security tool assets
  ansible.builtin.assert:
    that:
      - _latest_age_asset | default({}) | length > 0
      - _latest_sops_asset | default({}) | length > 0
      - _latest_cosign_asset | default({}) | length > 0
      - _latest_step_asset | default({}) | length > 0
      - _latest_age_asset.digest | default('') is match('^sha256:[0-9a-f]{64}$')
      - _latest_sops_asset.digest | default('') is match('^sha256:[0-9a-f]{64}$')
      - _latest_cosign_asset.digest | default('') is match('^sha256:[0-9a-f]{64}$')
      - _latest_step_asset.digest | default('') is match('^sha256:[0-9a-f]{64}$')
    fail_msg: "A current upstream security-tool asset could not be resolved"

# --- age (PQ support) ---
- name: Install latest age and age-keygen
  ansible.builtin.get_url:
    url: "{{ _latest_age_asset.browser_download_url }}"
    dest: "{{ workstation_update_artifact_cache_dir }}/age-{{ _latest_age_asset.digest | regex_replace('^sha256:', '') }}.archive"
    checksum: "{{ _latest_age_asset.digest }}"
    mode: "0644"
    force: false

- name: Extract latest age and age-keygen
  ansible.builtin.unarchive:
    src: "{{ workstation_update_artifact_cache_dir }}/age-{{ _latest_age_asset.digest | regex_replace('^sha256:', '') }}.archive"
    dest: /usr/local/bin
    remote_src: true
    include: ["*/age", "*/age-keygen"]
    extra_opts: [--wildcards, --strip-components=1]

# --- SOPS ---
- name: Install latest SOPS
  ansible.builtin.get_url:
    url: "{{ _latest_sops_asset.browser_download_url }}"
    dest: "{{ workstation_update_artifact_cache_dir }}/sops-{{ _latest_sops_asset.digest | regex_replace('^sha256:', '') }}"
    checksum: "{{ _latest_sops_asset.digest }}"
    mode: "0644"
    force: false

- name: Atomically install latest SOPS
  ansible.builtin.copy:
    src: "{{ workstation_update_artifact_cache_dir }}/sops-{{ _latest_sops_asset.digest | regex_replace('^sha256:', '') }}"
    dest: /usr/local/bin/sops
    remote_src: true
    mode: "0755"

- name: Remove ~/go/bin/sops shadow (outdated alternative)
  ansible.builtin.file:
    path: "{{ dev_user_home }}/go/bin/sops"
    state: absent

# --- PQ age keypair generation (per-user, idempotent) ---
# Uses ~/.keys/age.key (org convention). Skips if any age key already exists.
- name: Check if age key already exists
  ansible.builtin.stat:
    path: "{{ dev_user_home }}/.keys/age.key"
  register: age_key_check

- name: Ensure user .keys directory exists
  ansible.builtin.file:
    path: "{{ dev_user_home }}/.keys"
    state: directory
    owner: "{{ dev_user }}"
    group: "{{ dev_user }}"
    mode: "0700"
  when: not age_key_check.stat.exists

- name: Generate PQ age keypair (one-time)
  ansible.builtin.command:
    cmd: age-keygen -o {{ dev_user_home }}/.keys/age.key
    creates: "{{ dev_user_home }}/.keys/age.key"
  become: true
  become_user: "{{ dev_user }}"
  environment:
    HOME: "{{ dev_user_home }}"

- name: Set age key file permissions
  ansible.builtin.file:
    path: "{{ dev_user_home }}/.keys/age.key"
    owner: "{{ dev_user }}"
    group: "{{ dev_user }}"
    mode: "0600"
  when: not age_key_check.stat.exists

- name: Extract and display age public key (first run only)
  when: not age_key_check.stat.exists
  block:
    - name: Extract age public key
      ansible.builtin.shell:
        cmd: |
          set -o pipefail
          grep 'public key' {{ dev_user_home }}/.keys/age.key | cut -d' ' -f4
        executable: /bin/bash
      register: age_public_key
      changed_when: false

    - name: Write public key to ~/.keys/age.pub
      ansible.builtin.copy:
        content: "{{ age_public_key.stdout }}\n"
        dest: "{{ dev_user_home }}/.keys/age.pub"
        owner: "{{ dev_user }}"
        group: "{{ dev_user }}"
        mode: "0644"

    - name: Display age public key
      ansible.builtin.debug:
        msg: >-
          PQ age public key for {{ dev_user }}:
          {{ age_public_key.stdout }}
          — Send this to the infrastructure admin to be added as a SOPS recipient.

# --- cosign ---
- name: Install latest cosign
  ansible.builtin.get_url:
    url: "{{ _latest_cosign_asset.browser_download_url }}"
    dest: "{{ workstation_update_artifact_cache_dir }}/cosign-{{ _latest_cosign_asset.digest | regex_replace('^sha256:', '') }}"
    checksum: "{{ _latest_cosign_asset.digest }}"
    mode: "0644"
    force: false

- name: Atomically install latest cosign
  ansible.builtin.copy:
    src: "{{ workstation_update_artifact_cache_dir }}/cosign-{{ _latest_cosign_asset.digest | regex_replace('^sha256:', '') }}"
    dest: /usr/local/bin/cosign
    remote_src: true
    mode: "0755"

# --- step-cli ---
- name: Download latest step-cli
  ansible.builtin.get_url:
    url: "{{ _latest_step_asset.browser_download_url }}"
    dest: "{{ workstation_update_artifact_cache_dir }}/step-{{ _latest_step_asset.digest | regex_replace('^sha256:', '') }}.archive"
    checksum: "{{ _latest_step_asset.digest }}"
    mode: "0644"
    force: false

- name: Install latest step-cli
  ansible.builtin.unarchive:
    src: "{{ workstation_update_artifact_cache_dir }}/step-{{ _latest_step_asset.digest | regex_replace('^sha256:', '') }}.archive"
    dest: /usr/local/bin
    remote_src: true
    include: ["*/bin/step"]
    extra_opts: [--wildcards, --strip-components=2]

# =============================================================================
# Free/Open-Source Security Hardening Stack
# =============================================================================
# All packaged/free — no Canonical Pro account needed. CIS content is
# scan-only here: openscap can auto-remediate, but doing that unattended on a
# workstation risks disabling services or changing permissions in ways nobody
# reviewed. We install the scanner + content and generate a report; applying
# remediation is a separate, human-reviewed step.
# =============================================================================

- name: Install needrestart (auto-restart services after lib upgrades)
  ansible.builtin.apt:
    name: needrestart
    state: present

- name: Install debsecan (Debian/Ubuntu vulnerability scanner)
  ansible.builtin.apt:
    name: debsecan
    state: present

- name: Install AIDE (file integrity monitoring)
  ansible.builtin.apt:
    name:
      - aide
      - aide-common
    state: present

# AIDE's stock config (99_aide_root: "/ 0 Full") is a deliberately
# unrestricted catch-all that checksums the ENTIRE filesystem, including every
# user's home directory. On a real developer workstation that's the wrong
# scope: home directories here hold gigabytes of node_modules/venvs/container
# images/personal documents with near-zero intrusion-detection value per byte
# hashed. A from-scratch init against this box's /home ran 30+ minutes,
# peaked at 7.9GB RAM, and still didn't finish.
#
# AIDE's actual value is protecting *system* integrity (binaries, configs) —
# so we prune all of /home from the catch-all and re-add a small set of
# genuinely security-relevant paths within it. AIDE uses a deepest-match
# algorithm (see aide.conf(5)), so a more specific rule below still applies
# even though a shorter prefix was pruned above it.
#
# Important AIDE syntax notes, verified via `aide --config-check`, not
# assumed from generic docs:
#   1. "!" (recursive negative rule) only excludes the matched entry itself —
#      AIDE still recurses into its children. Our first attempt used "!" on
#      directories like ~/.claude and it kept scanning everything inside
#      them anyway. "-" (non-recursive negative rule, AIDE v0.19+) is the one
#      that actually prunes — no descent at all.
#   2. Regex must start with a literal "/" — a bare ".*" as the first
#      character breaks the parser ("unexpected character '!'" reported on
#      the *next* line, not this one — a confusing error to track down).
#   3. Negative-rule lines need a trailing file-type letter ("d" for
#      directory), matching every rule already shipped in
#      /etc/aide/aide.conf.d/*.
- name: Prune home directories from AIDE's default full-filesystem scan
  ansible.builtin.copy:
    dest: /etc/aide/aide.conf.d/zz_workstation_excludes
    owner: root
    group: root
    mode: "0644"
    content: |
      # Managed by Ansible — developer-workstation role
      -/home/[^/]+$ d
      -/var/lib/docker$ d
      -/var/lib/containers$ d
      -/var/lib/snapd$ d
      -/snap$ d
      # Swap file content is meaningless to integrity-monitor (changes every
      # boot) and, at 16G on this host, was the single largest contributor
      # to AIDE's scan time/memory — found via `find / -xdev -size +200M`,
      # not guessed.
      -/swap\.img$ f

      # Re-include specific security-relevant files despite the /home prune
      # above — AIDE's deepest-match means these more specific rules win.
      /home/[^/]+/\.ssh$ d R
      /home/[^/]+/\.ssh/authorized_keys$ f R
      /home/[^/]+/\.ssh/config$ f R
      /home/[^/]+/\.bashrc$ f R
      /home/[^/]+/\.bash_profile$ f R
      /home/[^/]+/\.profile$ f R
      /home/[^/]+/\.zshrc$ f R

- name: Check if AIDE database is initialized
  ansible.builtin.stat:
    path: /var/lib/aide/aide.db
  register: _aide_db

- name: Deploy asynchronous AIDE database initializer
  ansible.builtin.copy:
    dest: /etc/systemd/system/curiosity-aide-init.service
    owner: root
    group: root
    mode: "0644"
    content: |
      [Unit]
      Description=Initialize the AIDE integrity database
      ConditionPathExists=!/var/lib/aide/aide.db
      After=local-fs.target
      Before=dailyaidecheck.service

      [Service]
      Type=oneshot
      ExecStart=/usr/sbin/aideinit -y -f
      Nice=19
      IOSchedulingClass=idle
      IOSchedulingPriority=7
      TimeoutStartSec=infinity

      [Install]
      WantedBy=multi-user.target
  when: not _aide_db.stat.exists

- name: Ensure the daily AIDE check drop-in directory exists
  ansible.builtin.file:
    path: /etc/systemd/system/dailyaidecheck.service.d
    state: directory
    owner: root
    group: root
    mode: "0755"
  when: not _aide_db.stat.exists

- name: Guard the daily AIDE check until its database exists
  ansible.builtin.copy:
    dest: /etc/systemd/system/dailyaidecheck.service.d/10-curiosity-database.conf
    owner: root
    group: root
    mode: "0644"
    content: |
      [Unit]
      ConditionPathExists=/var/lib/aide/aide.db
  when: not _aide_db.stat.exists

- name: Reload systemd after deploying AIDE initialization controls
  ansible.builtin.systemd_service:
    daemon_reload: true
  when: not _aide_db.stat.exists

- name: Start asynchronous AIDE database initialization
  ansible.builtin.systemd_service:
    name: curiosity-aide-init.service
    enabled: true
    state: started
    no_block: true
  when: not _aide_db.stat.exists

- name: Install auditd (audit daemon)
  ansible.builtin.apt:
    name: auditd
    state: present

- name: Ensure auditd is enabled and running
  ansible.builtin.systemd:
    name: auditd
    enabled: true
    state: started

- name: Install rkhunter (rootkit hunter)
  ansible.builtin.apt:
    name: rkhunter
    state: present

- name: Update rkhunter file properties baseline (first run only)
  ansible.builtin.command: rkhunter --propupd
  args:
    creates: /var/lib/rkhunter/db/rkhunter.dat

- name: Install fail2ban (paired with ufw — see firewall.yml)
  ansible.builtin.apt:
    name: fail2ban
    state: present

- name: Configure fail2ban sshd jail (ships with nothing enabled by default)
  ansible.builtin.copy:
    dest: /etc/fail2ban/jail.local
    owner: root
    group: root
    mode: "0644"
    content: |
      # Managed by Ansible — developer-workstation role
      [sshd]
      enabled = true
      backend = systemd
  notify: Restart fail2ban

- name: Ensure fail2ban is enabled and running
  ansible.builtin.systemd:
    name: fail2ban
    enabled: true
    state: started

# canonical/sec-cvescan is deliberately NOT installed here. The repo is
# archived and its own README says: "CVEScan is not supported - instead it
# is recommended to use one of the available data formats." It's also only
# distributed as a snap (snapcraft.io/cvescan), not via pip — the PyPI
# package literally named "cvescan" is an unrelated third-party tool that
# scans dependency lockfiles (requirements.txt/pyproject.toml), not Ubuntu
# system packages. An earlier version of this task installed that wrong
# package by name collision; installing deprecated, unsupported security
# tooling (under the wrong name, no less) isn't a real substitute for this
# catalog item.

# --- CIS hardening content — scan/report only, no auto-remediation ---
- name: Install OpenSCAP scanner and Debian-derived CIS content
  ansible.builtin.apt:
    name:
      - openscap-scanner
      - ssg-debderived
    state: present

- name: Ensure OpenSCAP report directory exists
  ansible.builtin.file:
    path: /var/log/openscap
    state: directory
    owner: root
    group: root
    mode: "0750"

- name: Find installed SCAP Security Guide datastreams for this distro
  ansible.builtin.find:
    paths: /usr/share/xml/scap/ssg/content
    # ssg-debderived ships one datastream per supported release, e.g.
    # ssg-ubuntu2404-ds.xml, ssg-ubuntu2204-ds.xml — often several at once.
    patterns: "ssg-{{ ansible_distribution | lower }}*-ds.xml"
  register: _ssg_datastream

- name: Pick the datastream matching this host's release, falling back to newest available
  ansible.builtin.set_fact:
    _ssg_datastream_path: >-
      {%- set candidates = _ssg_datastream.files | map(attribute='path') | list -%}
      {%- set exact_suffix = 'ssg-' + (ansible_distribution | lower) + (ansible_distribution_version | replace('.', '')) + '-ds.xml' -%}
      {%- set exact = candidates | select('match', '.*' + exact_suffix) | list -%}
      {{ exact[0] if exact else (candidates | sort | last | default('')) }}

- name: Create weekly CIS/OpenSCAP evaluation script (report only, no --remediate)
  ansible.builtin.copy:
    dest: /usr/local/bin/openscap-weekly-eval.sh
    owner: root
    group: root
    mode: "0755"
    content: |
      #!/bin/bash
      # Managed by Ansible — developer-workstation role
      # Report-only: generates findings for human review. Does NOT remediate —
      # unattended CIS remediation can disable services this workstation needs.
      set -uo pipefail
      DATASTREAM="{{ _ssg_datastream_path }}"
      if [ -z "$DATASTREAM" ]; then
        logger -t openscap-weekly "No SCAP datastream found for {{ ansible_distribution }} — skipping"
        exit 0
      fi
      oscap xccdf eval \
        --profile xccdf_org.ssgproject.content_profile_cis_level1_workstation \
        --results /var/log/openscap/results-$(date +%Y%m%d).xml \
        --report /var/log/openscap/report-$(date +%Y%m%d).html \
        "$DATASTREAM" || true
      logger -t openscap-weekly "CIS evaluation complete — see /var/log/openscap/report-$(date +%Y%m%d).html"

- name: Create weekly OpenSCAP eval systemd service
  ansible.builtin.copy:
    dest: /etc/systemd/system/openscap-weekly-eval.service
    owner: root
    group: root
    mode: "0644"
    content: |
      [Unit]
      Description=Weekly CIS/OpenSCAP evaluation (report only)

      [Service]
      Type=oneshot
      ExecStart=/usr/local/bin/openscap-weekly-eval.sh
  notify: Reload systemd

- name: Create weekly OpenSCAP eval timer
  ansible.builtin.copy:
    dest: /etc/systemd/system/openscap-weekly-eval.timer
    owner: root
    group: root
    mode: "0644"
    content: |
      [Unit]
      Description=Weekly CIS/OpenSCAP evaluation

      [Timer]
      OnCalendar=Sun *-*-* 05:00:00
      RandomizedDelaySec=1h
      Persistent=true

      [Install]
      WantedBy=timers.target
  notify: Reload systemd

- name: Enable weekly OpenSCAP eval timer
  ansible.builtin.systemd:
    name: openscap-weekly-eval.timer
    enabled: true
    state: started
    daemon_reload: true

- name: Create weekly debsecan + rkhunter scan script
  ansible.builtin.copy:
    dest: /usr/local/bin/vuln-scan-weekly.sh
    owner: root
    group: root
    mode: "0755"
    content: |
      #!/bin/bash
      # Managed by Ansible — developer-workstation role
      set -uo pipefail
      debsecan --suppress-msg --format summary 2>/dev/null | logger -t debsecan-weekly || true
      rkhunter --check --sk --nocolors 2>/dev/null | logger -t rkhunter-weekly || true

- name: Create weekly vuln-scan systemd service
  ansible.builtin.copy:
    dest: /etc/systemd/system/vuln-scan-weekly.service
    owner: root
    group: root
    mode: "0644"
    content: |
      [Unit]
      Description=Weekly debsecan + rkhunter scan

      [Service]
      Type=oneshot
      ExecStart=/usr/local/bin/vuln-scan-weekly.sh
  notify: Reload systemd

- name: Create weekly vuln-scan timer
  ansible.builtin.copy:
    dest: /etc/systemd/system/vuln-scan-weekly.timer
    owner: root
    group: root
    mode: "0644"
    content: |
      [Unit]
      Description=Weekly debsecan + rkhunter scan

      [Timer]
      OnCalendar=Wed *-*-* 05:00:00
      RandomizedDelaySec=1h
      Persistent=true

      [Install]
      WantedBy=timers.target
  notify: Reload systemd

- name: Enable weekly vuln-scan timer
  ansible.builtin.systemd:
    name: vuln-scan-weekly.timer
    enabled: true
    state: started
    daemon_reload: true
