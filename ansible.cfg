[defaults]
roles_path = roles
host_key_checking = False
# The `community.general.yaml` callback was removed in community.general 12.0.
# ansible-core 2.13+ reimplements YAML-formatted output via result_format.
stdout_callback = default
result_format = yaml
